"use client";

import CustomizeInterface from "@/components/features/customize/CustomizeInterface";

export default function CustomizePage() {
    return (
        <main className="h-screen bg-slate-900 overflow-hidden">
            <CustomizeInterface />
        </main>
    );
}
